#!/usr/bin/env bash
# Stub: simulates Codex resuming an imported session.
# Used exclusively by the VHS demo recording.
# Called as: codex resume <uuid>

UUID="${2:-unknown}"

printf '\n'
printf '  ╭──────────────────────────────────────────╮\n'
printf '  │  Codex  (gpt-4o)                          │\n'
printf '  ╰──────────────────────────────────────────╯\n'
printf '\n'
printf '  Resuming session %s\n' "$UUID"
printf '\n'
sleep 0.4
printf '  > (context imported from Claude Code)\n'
printf '\n'
sleep 0.3
printf '  ● Picking up where you left off.\n'
printf '    I can see the rate limiting and JWT auth work.\n'
printf '    What would you like to do next?\n'
printf '\n'
